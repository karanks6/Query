import 'package:flame/components.dart';
import 'query_scene.dart';

class OnboardingScene extends QueryScene {
  @override
  Future<void> onEnter() async {
    // Zoom in parallax or change colors
  }
}
